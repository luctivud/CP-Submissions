class ChessBoard {
    int ChessBoard[8][8];

    makemove(currentPosition, nextPosition) {
        // get the information of type of piece at currentPosition
        switch (case ) {

        }
        if checkValid() {

        } else {

        }
    }
}




class Piece {
    pair<int, int> currentPosition;
    pair<int, int> nextPosition;
    interface checkValid(nextPosition) 
}

class Queen inherit Piece implement checkValid {
    checkValid(currentPosition, nextPosition) {

    }
}

class King inherit Piece implement checkValid {
    checkValid(currentPosition, nextPosition) {

    }
}


//  

// clock = 0
// i