
#include <bits/stdc++.h>
using namespace std;

typedef long long int lld;

class LongestPalindrome {
	static isPalindrome
  public:
	int longestLength(string s) {
		int n = s.length();

	}
};




void readNumber(lld &N) {
  cin >> N;
}

void readVector(vector<lld> arr) {
  for (auto &i : arr) {
    cin >> i;
  }
}



signed main() {
  LongestPalindrome obj;

  // lld N; vector<lld> arr(N);
  // readNumber(N); readVector(arr);

  cout << obj.longestLength();


  return 0;
}
